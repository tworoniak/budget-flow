# Current Feature

## Status

Not Started

## Goals

<!-- Document the feature goals here -->

## Notes

<!-- Additional context, constraints, or details -->

## History

- Project setup and boilerplate cleanup
- Phase 1 MVP plan documented in `context/plan/phase1-mvp.md`
- **Foundation setup** (`feature/foundation`): TypeScript types, SCSS architecture, Zustand stores (localStorage persist), React Router, AppLayout + Sidebar, page placeholder shells. Boilerplate cleared.
- **Expense CRUD** (`feature/expense-crud`): Modal (Framer Motion), ExpenseForm (React Hook Form + Zod), ExpenseRow (color-coded categories, hover actions), ExpenseFilters (search, category, date range), ExpenseList, full ExpensesPage. Installed @hookform/resolvers.
- **Design refresh** (`feature/design-refresh`): Extended sidebar (more nav items, profile card, on-track badge). Expenses redesigned to dual-panel (list + SpendingPanel), tab filters (All/This week/Last month/Recurring), icon badges on rows, Export button.
- **Budget management** (`feature/budget-management`): BudgetPage with income inline-edit, 4-stat summary row (income, budgeted, unallocated, savings rate), BudgetCategoryCard (colored badge, progress bar, overspend warning), BudgetForm (RHF + Zod, duplicate prevention), budgetCalculations utils.
- **Dashboard** (`feature/dashboard`): Greeting hero, 4 summary cards with sparklines, cumulative spend AreaChart (period toggle), budget breakdown panel, recent transactions list, spending-by-category donut chart, useDashboardData hook. Added lucide-react — replaced all text/emoji icons across Sidebar, Modal, ExpenseRow, BudgetCategoryCard, and page action buttons.
- **CSV export & import** (`feature/csv-export`): csvExport.ts generates properly escaped CSV (id,title,amount,category,date,notes,recurring). csvImport.ts parses with custom quoted-field parser + Zod row validation with per-row error reporting. ImportModal with drag-and-drop + click-to-browse, preview table (first 10 rows), error list. importExpenses store action dedupes by id. Export wired on Expenses and Dashboard pages.
- **Advanced filtering** (`feature/advanced-filtering`): FilterState extended with minAmount, maxAmount, sortBy (date|amount|title), sortDir (asc|desc). ExpenseFilters gains amount range inputs and sort dropdown with direction toggle. ExpenseList applies sort after filtering. ActiveFilterChips renders a removable pill chip per active filter with a Clear all button.
- **Recurring expenses** (`feature/recurring-expenses`): recurringExpenses.ts generates missing monthly instances (deduped by title+category, idempotent). useExpenseStore gains lastRecurringCheck + setLastRecurringCheck. useRecurringCheck hook runs once per calendar month from AppLayout. RecurringList dedupes templates, shows Monthly badge/last date/edit/stop actions. RecurringPage added. /recurring route enabled. Sidebar Recurring nav item activated.
- **Animations** (`feature/animations`): AppLayout wraps Outlet in AnimatePresence mode="wait" + motion.div keyed on pathname for fade+slide page transitions. ExpenseList wraps each row in motion.div with layout + staggered entrance (cap at 5 items × 40ms) + animated exit. SummaryCard uses useMotionValue + useSpring + useTransform for number counting on mount and value change. BudgetCategoryCard progress bar uses 0.6s spring cubic-bezier.
- **Budget forecasting** (`feature/budget-forecasting`): forecasting.ts utilities (calcDailyAverage, calcProjectedMonthly, buildForecastLine). useDashboardData extended with dailyAverage, forecastedMonthlySpend, forecastLine. CumulativeSpendChart merges forecast data + renders dashed gray Area (this-month only). ForecastBadge shows amber "on track" or red "overspend" pill below dashboard greeting. Sidebar changed to position:fixed; AppLayout switched to block layout with margin-left offset.
- **Command palette** (`feature/command-palette`): Cmd+K/Ctrl+K global palette. useCommandPalette hook (global keydown listener). useCommands hook builds 7 commands (Navigation: Dashboard/Expenses/Budget/Recurring; Actions: Add expense/Export CSV/Clear filters). CommandPalette component with search, grouped results, keyboard nav (↑↓ Enter Esc). Wired into AppLayout with root-level quick-add Modal. Also fixed off-by-one date bug: parseLocalDate() in dateUtils.ts parses YYYY-MM-DD as local midnight; applied across all 7 createdAt conversion sites. Calendar picker icon made visible via CSS filter on dark background.
- **Sidebar + TopBar redesign** (`feature/sidebar-header-redesign`): Sidebar restructured into MAIN / INSIGHTS / WORKSPACE labelled sections. Sticky TopBar added inside `<main>`: breadcrumb (Workspace > Page), global search button (opens Cmd+K palette), right-side action slot. TopBarActionsContext lets each page inject its own action buttons (Export/Import/Add/etc.) via useEffect. AppLayout restructured to flex column with mainContent wrapper; TopBar stretches full-width with its own horizontal padding.
- **Right-side drawer** (`feature/right-side-drawer`): Drawer.tsx replaces Modal for expense add/edit on Dashboard, Expenses, and AppLayout quick-add. Slides from right (480px, full height) with dimming backdrop, Esc-to-close, backdrop-click-to-close. ExpenseForm redesigned: large centered amount input, uppercase field labels, 8-category icon grid (replaces dropdown), optional tags pill-chip input (Enter/comma to add), "Save & new" resets form and keeps drawer open. `tags?: string[]` added to Expense type. ImportModal stays as centered modal. Drawer accepts `variant?: 'right' | 'bottom'` for Phase 3 Branch 4 mobile reuse.
- **Empty states** (`feature/empty-states`): Dashboard shows full welcome + 5-step checklist (Launch/Income/Category/Expense/5+ expenses, derived from store state) when no expenses. "Skip tour" switches to minimal "Nothing tracked yet" icon + CTAs, persisted via `hasSkippedOnboarding` in budget store. Expenses page shows stacked-receipt illustration + "Your money story starts here" + Import/Add CTAs + 3 tip cards when no expenses. Existing filter no-results message unchanged. ImportModal added to DashboardPage for the Import CTA. SetupChecklist is a reusable component with progress bar.
- **Mobile layout** (`feature/mobile-layout`): BottomTabBar (Home/Expenses/FAB/Budgets/More) rendered via createPortal, fixed to viewport, hidden on desktop. MobileDashboard component (hero spend, 3 mini stats, budget list). ExpenseListGrouped (date-grouped rows). MobileFilterSheet (bottom-sheet filter panel). Drawer auto-switches to bottom sheet on mobile via useIsMobile. MobileMoreActionsContext lets pages inject Export/Import into the More sheet. Sidebar hidden at ≤768px. All mobile breakpoints unified at 768px. TopBar search and secondary buttons hidden at ≤1199px. Removed overflow-x:hidden from layout (was breaking position:fixed).
- **Critical audit fixes** (`fix/critical-audit-fixes`): Fixed timezone bug in `useDashboardData` last-month filter (was using `new Date()` instead of `parseLocalDate()`). Removed two setState-in-effect anti-patterns in `CommandPalette` (state reset moved to render-time check + onChange handler, setTimeout focus hack removed). Added `isNew` prop to `Drawer` so the NEW badge only renders when adding an expense, not when editing.
- **High-priority audit fixes** (`fix/high-priority-audit`): Added `sr-only` labels to all ExpenseFilters inputs. Replaced `div[role=button]` with a semantic `<button>` in ExpensesEmptyState. Added `useFocusTrap` hook + `aria-modal` + scroll lock to Drawer and Modal. Lazy-loaded all four route pages with Suspense (bundle split from 1 MB monolith to 574 kB shared + per-route chunks). Added top-level ErrorBoundary to prevent blank-screen crashes. Removed four duplicate CATEGORY_COLORS constants and consolidated to CATEGORY_CONFIG.
- **Medium-priority audit fixes** (`fix/medium-priority-audit`): Replaced hardcoded "Alex Dean" with "BudgetFlow" in Sidebar profile card. Added toast when recurring expenses auto-generate each month. Prepended UTF-8 BOM to CSV export for Excel/Windows compatibility. Added 5 MB file size guard to ImportModal before FileReader. Swapped RecurringPage Modal → Drawer. Fixed SummaryCard sparkline SVG gradient ID collision via useId(). Fixed SpendingByCategory donut chart to use this-month data instead of all-time.
- **Phase 4 — Data Model** (`feature/phase4-data-model`): Migrated from Vite + React Router to Next.js 16 App Router. All `react-router-dom` APIs replaced with `next/navigation` (`usePathname`, `useRouter`, `Link`). `src/pages/` renamed to `src/views/` to avoid Pages Router detection. Added `'use client'` to all interactive components. Zustand `persist` stores updated with `skipHydration: true` + client-side rehydration in AppLayout. Fixed `useIsMobile` SSR crash (`window.innerWidth` guarded). Replaced `useState + useEffect` mount guard in `BottomTabBar` with `useSyncExternalStore` (avoids react-hooks/set-state-in-effect, idiomatic React 18+). Set up Prisma 7 with full data model schema (User, Expense, BudgetCategory, Tag, ExpenseTag, NextAuth models) in `prisma/schema.prisma`. Connection config moved to `prisma.config.ts` (Prisma 7 breaking change). Neon PostgreSQL connection via `@prisma/adapter-neon`. Initial migration `20260518233610_init` applied to Neon dev database. Prisma client singleton at `src/lib/prisma.ts`. ESLint flat config rewritten to remove Vite-specific `react-refresh` plugin.
- **Auth core — Phase A** (`feature/auth-core`): Installed `next-auth@beta` (v5.0.0-beta.31) + `@auth/prisma-adapter`. Split auth config pattern for edge compatibility: `src/lib/auth.config.ts` (Google + GitHub providers, `authorized` callback, JWT/session callbacks — no Prisma imports) and `src/lib/auth.ts` (PrismaAdapter + JWT session strategy, spreads authConfig). Route handler at `src/app/api/auth/[...nextauth]/route.ts`. Middleware at `src/proxy.ts` (must be under `src/` when using `src/app/` convention). Session type extended with `user.id` in `src/types/next-auth.d.ts`. All app routes protected; `/sign-in` and `/register` are public. Consolidated project to standard Next.js `src/` convention: moved root `app/` into `src/app/` (routing files now colocated with store and commands under `src/`). Smoke tested: unauthenticated redirect → Google/GitHub sign-in → redirect back to app. User + Account rows created in Neon on first OAuth sign-in.
- **Auth credentials — Phase B** (`feature/auth-credentials`): Installed `bcryptjs` (pure-JS, no native bindings). Added Credentials provider placeholder to `auth.config.ts` (edge-safe) with named `credentials` fields so NextAuth default page renders the email/password form. Overrode Credentials in `auth.ts` with real bcrypt hash comparison (rounds=12). Added `POST /api/auth/register` with input validation, duplicate email check (409), and User creation. Fixed `src/proxy.ts` matcher to exclude `/.well-known/*` — Chrome DevTools auto-requests this path and was being captured as `callbackUrl`, causing post-login redirect to the DevTools JSON endpoint.
- **Auth UI — Phase C** (`feature/auth-ui`): Custom `/sign-in`, `/register`, `/forgot-password` pages with two-panel dark layout (left: hero copy + testimonial, right: OAuth + email form). Route groups `(app)/` and `(auth)/` isolate AppLayout from auth pages. SessionProvider added to `(app)/layout.tsx` with server-side session pre-fetch. `auth.config.ts` updated: `pages.signIn`, `/forgot-password` in PUBLIC_PATHS, logged-in users redirected away from auth pages, OAuth `profile.picture` stored in JWT. Sidebar profile card replaced with real user name/email/avatar (OAuth image or color-hashed initials) + sign-out dropdown. Google + GitHub avatar hostnames added to `next.config.ts`. Fixed Next.js 16 proxy convention (`src/proxy.ts` is the correct middleware file, not `middleware.ts`).
- **Cloud sync** (`feature/cloud-sync`): Migrated all expense and budget data from localStorage to Neon PostgreSQL, scoped per user. Removed `persist` middleware from `useExpenseStore` and `useBudgetStore`; replaced with DB-backed `fetchExpenses`/`fetchBudget` loaders and optimistic mutations with rollback on error. Created `useLocalStore` for UI-only prefs (`lastRecurringCheck`, `hasSkippedOnboarding`) that stay in localStorage. Added `src/lib/apiAuth.ts` shared `requireUserId()` guard. Built 7 API routes: GET+POST `/api/expenses`, PUT+DELETE `/api/expenses/[id]`, POST `/api/expenses/import`, GET `/api/budget`, PUT `/api/budget/income`, POST `/api/budget/categories`, PUT+DELETE `/api/budget/categories/[id]`. AppLayout now calls `fetchExpenses` + `fetchBudget` on mount. `useRecurringCheck` and `DashboardEmptyState` updated to read from `useLocalStore`.
